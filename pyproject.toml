[tool.pytest.ini_options]
minversion = "6.0"
testpaths = [
    "backend",
]

[tool.coverage.run]
source = [
    "gn_plugin_depobio",
]
omit = [
    "*/tests/*",
    "*/migrations/*",
]

[tool.black]
line-length = 99
